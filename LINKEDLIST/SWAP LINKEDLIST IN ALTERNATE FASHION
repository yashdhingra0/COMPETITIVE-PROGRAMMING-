ListNode* Solution::swapPairs(ListNode* A) {
ListNode* curr=A;
if(A->next==NULL){
return A;
}
while(curr && curr->next){
int temp=0;
temp = curr->val;
curr->val=curr->next->val;
curr->next->val = temp;
curr = curr->next->next;

}
return A;

}
